# 卸载指南

本文档描述如何完整卸载 My_Agent MSA 项目的运行时部署，**假定用户是最复杂的情况**（全部 13 个服务 + Istio + NFS + tool-runtime WSL 组件 + 端口转发均已部署）。

**安全原则：只清理本项目自己创建/部署的内容，不误伤用户其他结构。**

| 不会动的东西 | 原因 |
|---|---|
| WSL2 发行版本身、Docker Desktop | 用户环境基础设施 |
| `kube-system` / `kube-public` / `default` 等系统 namespace | Kubernetes 系统资源 |
| 非 `my-agent-*` 前缀的 PV/PVC | 可能是用户其他业务 |
| `~/.nvm` 里安装的 clawhub / codex 本体 | 可能是用户自装工具，只删项目 wrapper |
| `nfs-kernel-server` / `openssh-server` 软件包 | 可能被其他用途使用，只删项目配置 |
| 仓库代码 `My_Agent_MSA/` | 纯代码，卸载不影响 |
| `authorized_keys` 中其他行 | 只删含 `my-agent-tool-runtime` 的行 |

> ⚠️ **数据不可逆警告**：NFS 目录 `/srv/nfs/my-agent`（config、workspace、openviking 向量库、用户数据）删除后不可恢复。卸载前务必先备份。

---

## 0. 卸载前备份（推荐）

```bash
# 在 WSL2 中执行，备份全部项目数据（可放到 Windows 能访问的位置）
sudo tar -czf /mnt/c/my-agent-backup-$(date +%Y%m%d).tar.gz /srv/nfs/my-agent
```

---

## 1. 停掉 Windows 侧进程

```powershell
# 结束本项目相关的 kubectl port-forward 进程
# 本项目典型占用：5210(gateway) 5102(image-assets) 5700(dashboard) 8080(istio)
Get-CimInstance Win32_Process -Filter "Name='kubectl.exe'" |
  Where-Object { $_.CommandLine -match 'port-forward' -and $_.CommandLine -match 'agent|istio-system' } |
  ForEach-Object { Stop-Process -Id $_.ProcessId -Force }

# 确认已全部结束（应无输出）
Get-CimInstance Win32_Process -Filter "Name='kubectl.exe'" | Select-Object ProcessId, CommandLine
```

- 关闭 admin-panel（WPF 窗口直接关）。其编译产物在 `admin-panel/bin`，可保留（重装时重新 `dotnet build`），也可手动删除。

---

## 2. 删除 Kubernetes 资源

```powershell
# 2.1 删除 agent 命名空间（包含全部 Deployment / Service / PVC / ConfigMap / Secret / Role）
kubectl delete namespace agent

# 等待命名空间完全消失（Terminating 卡住通常是 PVC finalizer，多等一会儿）
kubectl get ns agent
```

```powershell
# 2.2 删除集群级 PV（namespace 删除后 PV 会变为 Released，需手动删除）
#     只删 my-agent-* 前缀，避免误伤其他 PV
kubectl get pv | Select-String 'my-agent'
kubectl delete pv my-agent-assets-pv my-agent-config-pv my-agent-openviking-pv `
  my-agent-timer-tasks-pv my-agent-user-data-pv my-agent-workspace-pv
```

> 若之前部署过自定义 PV（非 `my-agent-*` 命名），请按实际名称逐一确认后删除，不要批量删。

### 2.3 Istio（仅当集群只跑本项目时才卸载）

`istio-system` 与项目同批部署，但它是集群级组件。**如果这台机器上还有其他业务使用 Istio，跳过本节。**

```powershell
# 先确认 istio-system 里没有其他业务的资源
kubectl get all -n istio-system

# 确认无误后卸载
kubectl delete -f deploy/services/istio/istio.yaml
kubectl delete namespace istio-system
```

---

## 3. 清理 Docker 镜像

只删本项目构建的 `agent/*` 镜像和 tool-runtime 引入的 `searxng/*`，不动其他镜像。

```powershell
docker images --format '{{.Repository}}:{{.Tag}}' |
  Where-Object { $_ -match '^agent/' -or $_ -match '^searxng/' } |
  ForEach-Object { docker rmi $_ }
```

> 历史版本镜像较多时逐个删除较慢，可接受；不要使用 `docker image prune -a`（会误删无关镜像）。

---

## 4. 清理 WSL2 / NFS（在 WSL2 终端中执行）

### 4.1 删除项目 NFS 数据目录（先确认已完成第 0 步备份）

```bash
sudo rm -rf /srv/nfs/my-agent
```

### 4.2 删除 NFS export 配置（文件名带 my-agent，只删这一份）

```bash
sudo rm -f /etc/exports.d/my-agent.exports
sudo exportfs -ra
```

### 4.3 清理 tool-runtime 执行环境残留

只删项目生成的文件，不动用户自己安装的 clawhub/codex 本体：

```bash
# 项目专用 SSH key（私钥 + 公钥）
rm -f ~/.ssh/my_agent_tool_runtime_ed25519 ~/.ssh/my_agent_tool_runtime_ed25519.pub

# 从 authorized_keys 中只移除含项目注释的那一行
sed -i '/my-agent-tool-runtime/d' ~/.ssh/authorized_keys

# 项目 wrapper 脚本
rm -f ~/.local/bin/my-agent-clawhub ~/.local/bin/my-agent-codex
```

> **不卸载** `nfs-kernel-server` 与 `openssh-server` 软件包：可能被其他用途使用。若确认只为本项目安装，可自行执行 `sudo apt remove -y nfs-kernel-server openssh-server`。

---

## 5. 验证卸载完成

```powershell
# K8s：agent / istio-system 命名空间应消失
kubectl get ns

# PV：不应再有 my-agent-* 残留（Released 状态也要确认删净）
kubectl get pv | Select-String 'my-agent'

# 镜像：agent/* 与 searxng/* 应清空
docker images --format '{{.Repository}}:{{.Tag}}' | Select-String '^agent/|^searxng/'

# 端口转发：不应再有相关进程
Get-CimInstance Win32_Process -Filter "Name='kubectl.exe'" | Select-Object ProcessId, CommandLine
```

```bash
# WSL2：NFS 目录应不存在
ls /srv/nfs/my-agent   # 应报 No such file or directory

# exports 应无 my-agent 条目
grep -r my-agent /etc/exports.d/ || echo "已清理"
```

---

## 卸载后剩余物说明（可选再清理）

| 剩余物 | 说明 |
|---|---|
| 仓库代码 `E:\github\My_Agent_MSA` | 保留；重装直接用 `deploy-all.ps1` |
| `admin-panel/bin` 编译产物 | 保留；重装时 `dotnet build` 会重新生成 |
| `~/.nvm` 里的 clawhub / codex | 用户工具本体，保留 |
| Docker 基础镜像（python 等） | 项目构建依赖，`docker image prune` 可清（注意会连同其他项目镜像一起） |
| WSL2 / Docker Desktop | 用户环境，保留 |
