# agent-orchestrator-service 上下文管理

## 架构总览

上下文管理分为三层：**运行时栈**（TaskRuntime）、**持久化存储**（ContextClient → openviking-context-service）、**模型调用组装**（AgentRuntime.send）。

---

## 1. 上下文栈 — `TaskRuntime` (`task_runtime.py`)

`TaskRuntime` 维护一个 `agent_context` 列表作为栈，支撑 agent 间的递归调用和正确返回。

### 核心字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `agent_context` | `list[dict]` | 上下文栈，每项 `{"from": obj, "input": str}` |
| `temp_dialog_input` | `Any` | agent 间传递输入数据的临时通道 |
| `temp_dialog_output` | `Any` | agent 间传递输出数据的临时通道 |
| `main_memory` | `list[str]` | 任务级记忆（仅 main agent 使用） |
| `task_memory` | `list[str]` | 任务特定记忆 |
| `tool_log` | `list[str]` | 工具执行日志 |
| `send_images` | `list[str]` | 最终要发送的图片 URL 列表 |
| `send_text` | `str` | 最终要发送的文本 |

### 关键方法

- **`push_context(from_obj, input_text)`** — 压栈，记录调用者和传入内容
- **`pop_context()`** — 弹栈，返回上一个调用者
- **`consume_temp_dialog_input()`** — 取出并清空临时输入
- **`consume_temp_dialog_output()`** — 取出并清空临时输出

### 初始化

创建 `TaskRuntime` 时自动压入初始上下文：

```python
self.push_context(self.user, self.content)
```

---

## 2. 持久化上下文 — `ContextClient` (`context_client.py`)

通过 gRPC 调用 `openviking-context-service`，实现跨会话的上下文存储和检索。

### `search_context()`

**参数**：`user_id`, `session_id`, `query`, `agent_id`, `top_k`, `max_tokens`, `max_messages`, `commit_limit`

**返回**：message 列表，包含三部分：

| 来源 | role | 格式 |
|------|------|------|
| `session_summary` | `system` | `【会话摘要】\n{摘要内容}` |
| `memories` | `system` | `【相关记忆】{记忆内容}` |
| `recent_messages` | 原 role | 最近对话消息 |

### `append_turn()`

**参数**：`user_id`, `session_id`, `task_id`, `user_message`, `assistant_message`, `agent_id`, `tool_summaries`, `commit_limit`

任务结束后持久化本轮对话到 OpenViking。

---

## 3. 上下文组装 — `AgentRuntime.send()` (`agent_runtime.py:367`)

每次调用模型时，`send()` 按以下固定顺序组装 messages：

```
┌─────────────────────────────────────────┐
│  long_context_message                   │  ← search_context() 结果
│  ├─ system: "以下是你和用户的历史对话..."   │     (会话摘要 + 记忆 + 最近消息)
│  └─ ... search_context 返回的消息 ...     │
├─────────────────────────────────────────┤
│  task_memory_messages  (仅 main agent)   │  ← task.main_memory
│  └─ system: "以下是你在本次任务中的记忆:"   │
├─────────────────────────────────────────┤
│  system_prompt_messages                 │  ← GLOBAL_SETTING.md
│  └─ agent 专属 prompt 文件               │     + config.files 中的 prompt
├─────────────────────────────────────────┤
│  user_input_messages                    │  ← 用户原始请求
│  ├─ system: "以下为本次请求对话..."         │
│  └─ user: <user_id>原始内容              │
├─────────────────────────────────────────┤
│  current_input_messages                 │  ← 本轮单次对话
│  ├─ system: "以下为本次单轮对话内容"        │     (可能来自其他 agent 的调用)
│  └─ user: <caller_id>内容               │
└─────────────────────────────────────────┘
```

组装完成后调用模型，然后进入 `parse_syntax()` 解析模型输出。

---

## 4. 主循环 — `process_task()` (`agent_runtime.py:200`)

```
process_task(task, emit)
│
├─ 1. first_call(task)
│     └→ 设置 default_agent
│     └→ default_agent.send(task, emit)       ← 首次调用，进入 send()
│
├─ 2. while agent_context 栈非空:             ← 弹栈循环
│     ├─ pop_context() → 获取调用者 + 栈输入
│     ├─ output = consume_temp_dialog_output() ← 获取上次 send() 的结果
│     │
│     ├─ 如果 target 是用户:
│     │     └→ _emit_user_message() → 最终输出，break
│     │
│     └─ 否则 (target 是 agent):
│           ├─ 拼装: stack_input + "【收到返回】" + output
│           ├─ set_temp_dialog_input(拼装结果)
│           └→ target.send(task, emit)        ← 再次进入 send()
│
└─ 3. 任务结束:
      ├─ append_turn() 持久化本轮对话
      └─ reader agent 总结 tool_log → 追加到 tool agent 上下文
```

**关键理解**：`send()` 不直接弹栈，而是在内部通过 `parse_syntax()` **压栈**。弹栈由 `process_task` 的 while 循环统一处理。

---

## 5. 工具调用上下文栈详解

这是整个上下文管理中最重要的流程。以一次工具调用为例：

### 5.1 时序图

```
process_task()                          send()                          parse_syntax()
    │                                       │                               │
    ├─ first_call ─────────────────────────→│                               │
    │                                       ├─ 组装 messages                 │
    │                                       ├─ 调模型，获得响应               │
    │                                       ├─ parse_syntax() ──────────────→│
    │                                       │                               ├─ 检测到 工具:xxx|args
    │                                       │                               ├─ push_context(agent,  【本轮已执行的工具】)
    │                                       │                               ├─ temp_dialog_output =  │
    │                                       │                               │    {tool_call: ...}     │
    │                                       │←──────────────────────────────│
    │                                       │                               │
    │                                       ├─ 发现 result["tool_call"]      │
    │                                       ├─ _run_tool() ───→ 执行工具     │
    │                                       ├─ temp_dialog_output = 工具结果  │
    │                                       ├─ return                       │
    │←──────────────────────────────────────│                               │
    │                                       │                               │
    ├─ while 循环: pop_context()            │                               │
    │   ├─ target = agent (压栈时的 agent)   │                               │
    │   ├─ stack_input = "【本轮已执行的工具】" │                               │
    │   ├─ output = consume_temp_dialog_output() = 工具结果                  │
    │   ├─ 拼装: "【本轮已执行的工具】"         │                               │
    │   │         + "【收到返回】"             │                               │
    │   │         + 工具结果                  │                               │
    │   └─ target.send(task, emit) ─────────→│  ← agent 再次调模型，看到工具结果
    │                                       ├─ 组装 messages (含工具结果)     │
    │                                       ├─ 调模型                        │
    │                                       ├─ 如果模型输出 对话:user|...     │
    │                                       │   └→ _emit_user_message()      │
    │                                       │       → 最终输出，break        │
```

### 5.2 压栈时机（`parse_syntax()` 中）

```python
# syntax_parser.py 中，检测到工具调用时：
tool_line = _find_command_block(full_text, "工具调用")
if tool_line:
    task.tool_log.append("调用了工具:" + tool_line)

    # 构造栈内容：本轮已执行的工具列表
    stack_parts = []
    if task.tool_log:
        stack_parts.append("【本轮已执行的工具】\n" + "\n".join(task.tool_log))

    task.push_context(agent, "\n".join(stack_parts))
    # ↑ 在工具执行前就压栈，记录"谁调用了工具"

    tool_call = _parse_tool_call(tool_line)
    # 将 tool_call 放入 temp_dialog_output 的 result 字典中
```

### 5.3 工具执行（`send()` 中，压栈后）

```python
# agent_runtime.py send() 方法中：
if result["tool_call"]:
    task.set_temp_dialog_output(result["tool_call"])
    self._run_tool(task, emit)
    # _run_tool 执行后，temp_dialog_output 被设为工具结果
    # send() 随后 return
```

### 5.4 弹栈与重发（`process_task()` 的 while 循环）

```python
# 弹栈
context = task.pop_context()
task.target = context["from"]     # 压栈时的 agent
stack_input = context["input"]    # "【本轮已执行的工具】\n..."
output = task.consume_temp_dialog_output()  # 工具结果

# 拼装后重新发送
parts = [stack_input, "【收到返回】\n" + output]
task.set_temp_dialog_input("\n\n".join(parts))
task.target.send(task, emit)      # agent 再次调模型，看到工具结果
```

### 5.5 多次工具调用的栈行为

如果 agent 在一次 send 中调用了多个工具（模型输出多行工具指令），每次都会压栈：

```
压栈 #1: agent 压入 tool_log_1
压栈 #2: agent 压入 tool_log_2  (tool_log 累积了之前的记录)
...

弹栈顺序 (LIFO):
  弹栈 #2 → output = 工具2结果 → send() → 模型看到 tool_log_1 + tool_log_2 + 结果2
  弹栈 #1 → output = 工具1结果 → send() → 模型看到 tool_log_1 + 结果1
```

---

## 6. Agent 间切换

### 对话切换 (`call_agent`)

模型输出 `对话:agent_id|内容` 时：

1. 检查目标是否为 `user`/`用户`/真实 user_id → 直接发送给用户
2. 否则：压栈当前状态 → 获取或创建目标 agent 实例 → `target.send(task, emit)`

### 与工具调用的区别

| | 工具调用 | Agent 切换 |
|---|---|---|
| 压栈位置 | `parse_syntax()` 内 | `call_agent()` 内 |
| 压栈内容 | `"【本轮已执行的工具】\n" + tool_log` | `"【已发送给{target}】\n" + content` |
| 执行 | `_run_tool()` 直接执行，结果写 `temp_dialog_output` | `target.send()` 进入完整的 send 流程 |
| 弹栈后 | 工具结果回传给原 agent | 目标 agent 的回复回传给原 agent |

---

## 7. 系统提示词构建 (`agent_config.py` + `build_system_prompt`)

### 配置加载

`load_agent_config()` 支持两种格式：

- **旧格式（平铺）**：`{"main": {...}, "tool": {...}}`
- **新格式（按用户）**：`{"default": {...}, "users": {"user_id": {...}}}`

用户配置通过 `deep_merge` 覆盖 default。

### 提示词组装

1. 读取 `GLOBAL_SETTING.md`（全局设定）
2. 按 `config.files` 列表依次读取 agent 专属 prompt 文件

所有文件内容作为独立 `system` 消息追加。

---

## 8. Agent 实例缓存

```python
_agent_instances: OrderedDict[str, AgentRuntime] = OrderedDict()
MAX_INSTANCES = 20
```

LRU 缓存，最多 20 个实例。key 格式为 `{agent_id}:{session_id}`。
