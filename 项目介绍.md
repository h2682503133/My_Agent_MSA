# 项目介绍

My_Agent_MSA 的技术细节与具体实现说明。快速上手请看根目录 [README.md](README.md)。

## 架构概览

```
Browser / QQ
  ↓ HTTP / Satori
frontend-service:80 (nginx)     qq-llbot-service (Satori → gRPC)
  ↓ /api/*                        ↓ gRPC
gateway-backend-service:5210 (FastAPI + SSE)
  ↓ gRPC                         ↓
task-scheduler-service:5100 (槽位调度 + 事件总线)
  ↓ gRPC
agent-orchestrator-service:5300 (Agent 运行时编排)
  ├── openviking-context-service:5301 (RAG 上下文检索，后端为 openviking-server:1933)
  ├── model-proxy-service:5302 (统一模型调用 → Moonshot / 本地 Ollama)
  ├── tool-runtime-service:5303 (工具执行 + workspace + skill)
  └── timer-task-service:5103 (定时任务，到期回调 scheduler)
user-service:5104 (用户信息与多渠道绑定)
dashboard-service:5700 (Web 管理面板)   image-assets-service (图床)   istio (入口网关 8080)
```

## 服务端口表

| 服务 | 端口 | 协议 | 说明 |
|------|------|------|------|
| frontend-service | 80 | HTTP | Nginx 静态页面 + `/api/*` 反代 gateway |
| gateway-backend-service | 5210 | HTTP | Web 后端网关，SSE 事件推送 |
| qq-llbot-service | — | Satori/gRPC | QQ 渠道接入（Satori 客户端，不监听端口） |
| qq-satori-adapter | 5600 | HTTP/WS | LLBot Satori 适配器（外部部署） |
| task-scheduler-service | 5100 | gRPC | 任务调度、槽位管理、事件总线 |
| timer-task-service | 5103 | gRPC | 定时任务管理，到期回调 scheduler |
| agent-orchestrator-service | 5300 | gRPC | Agent 运行时编排核心 |
| openviking-context-service | 5301 | gRPC | OpenViking RAG 上下文检索 |
| openviking-server | 1933 | HTTP | 语义检索服务（外部镜像 ghcr.io/volcengine/openviking） |
| model-proxy-service | 5302 | gRPC | 统一模型调用适配 |
| tool-runtime-service | 5303 | gRPC | 工具执行、skill、workspace |
| user-service | 5104 / 5204 | gRPC / HTTP | 用户信息、多渠道绑定、OpenViking key |
| dashboard-service | 5700 | HTTP | 管理控制面板（FastAPI + Web UI） |
| image-assets-service | 80 | HTTP | 图床（nginx，port-forward 5102 访问） |

## 核心功能

### 1. 多智能体编排

Agent 运行时支持 `main`、`tool`、`reader` 三种核心角色（可扩展自定义人设），通过 `agent_list.json` 配置模型参数与系统提示词。智能体之间可通过 `对话:target_id|content` 语法互相调用，也支持 `切换:agent_id` 改变默认智能体。

`agent_list.json` 支持按用户覆盖（`users` 段合并到 `default`），每个智能体可配置：

```text
model_profile   # 对应 model_list.json 中的模型配置（经 aliases 映射）
temperature     # 采样温度
commit_limit    # 上下文提交条数
max_tokens      # 输出上限
record_mode     # 记录模式（intermediate 等）
files           # 引用的 system_prompt 文件（SOUL.md / TOOL.md / ...）
```

### 2. 语法解析

模型输出通过 `syntax_parser.py` 解析，支持以下协议：

| 协议 | 格式 | 说明 |
|------|------|------|
| 对话 | `对话:target_id\|content` | 切换智能体或回复用户 |
| 工具调用 | `工具调用:tool_name\|arg1\|arg2` | 执行工具 |
| Shell 工具 | `工具调用:shell\|raw command` | 执行 shell 命令（优先级最高） |
| 切换智能体 | `切换:agent_id` 或 `切换到xxx智能体` | 更新默认智能体 |
| 定时任务 | `定时任务:类型\|agent_id\|内容\|开始时间\|重复计划` | 创建定时任务（重复计划可选，0=不重复） |
| 询问 | `询问:xxx` | 向用户提问 |

### 3. 定时任务

支持两种定时任务类型：

- **submit_task**：到期后通过 orchestrator 执行完整 Agent 链路
- **send_message**：到期后直接推送消息给用户（跳过 orchestrator，低延迟）

```text
定时任务:submit_task|[main]提交commit|2026-07-12 10:00:00|
定时任务:send_message|每日提醒内容|现在|每30分钟
```

**第 4 参 = 开始时间**（空=立即，也可写 `现在`）：

| 方式 | 示例 | 说明 |
|------|------|------|
| 绝对时间 | `2026-01-31 10:00:00` / `2026/01/31 10:00` | 指定时刻 |
| 当天时间 | `10:00` / `10点30分` / `下午3点` | 今天该时刻，已过则顺延明天 |
| 相对时间 | `5分钟后` / `2小时后` / `明天10:00` / `后天9点` | 从现在起算 |
| 区间随机 | `10:00-11:00`（当天随机时刻）/ `5-10分钟后`（随机延迟） | 一次性随机 |

**第 5 参 = 重复计划**（可选；空或 `0` = 不重复，一次性任务）：

| 方式 | 示例 | 说明 |
|------|------|------|
| 重复-单一时长 | `每10秒` / `每30分钟` / `每2小时` / `每天` | 到期按固定间隔重排 |
| 重复-混合单位 | `每1小时30分钟` | 多单位组合的固定间隔 |
| 重复-区间随机 | `每5-10分钟` / `每1-2小时` | 每次触发随机取区间内时长 |
| 重复-混合单位区间 | `每5分钟-2小时` / `每1小时30分钟-2小时` | 区间两端可用不同单位，每次随机取 |

### 4. 实时事件流

- gateway / qq-llbot 通过 gRPC 双向流订阅 scheduler 事件总线
- 前端通过 SSE（`/api/events`）接收实时推送
- 事件类型：`task_queued` → `task_started` → `assistant_message` / `assistant_intermediate` → `task_completed`

### 5. 多渠道接入

| 渠道 | channel 值 | 接入方式 |
|------|-----------|----------|
| Web | `web` | gateway-backend-service (FastAPI + SSE) |
| QQ | `qq` | qq-llbot-service (Satori + gRPC) |

`scheduler` 事件总线按 `channel` 字段路由，各渠道网关独立订阅各自频道，互不干扰。

### 6. 降级能力

| 场景 | 行为 |
|------|------|
| openviking-context-service 未部署 | orchestrator 自动降级为本地最近 4 回合上下文（`local_context.py`，用户+回复算 1 条） |
| tool-runtime 未勾选 codex / clawhub | 相关工具调用返回「未启用」提示（`ENABLE_CODEX` / `ENABLE_CLAWHUB` / `ENABLE_OPENVIKING` 开关） |
| openviking-server 未勾选 | skill 查询/导入返回「OpenViking 未启用」提示 |

## 部署

### 前置条件

- Kubernetes 集群（推荐 Docker Desktop + WSL2）
- Docker（构建镜像）、kubectl
- NFS 服务端（WSL 内 `deploy/setup-nfs.sh` 初始化）
- 模型 API Key（Moonshot 等，或本地 Ollama）
- LLBot / Satori 适配器（QQ 渠道需要）

### 一键部署（推荐，Windows）

项目根目录运行：

```powershell
powershell deploy-all.ps1
```

脚本能力：

- **必要服务组合**：默认勾选「必要服务」= Web 对话最小闭环（frontend / gateway-backend / task-scheduler / agent-orchestrator / model-proxy），Enter 直接部署
- **其余服务独立选项**：timer-task / qq-llbot / openviking / user / dashboard / tool-runtime / image-assets，按需勾选
- **tool-runtime 子选项**：codex（代码生成）、clawhub（技能执行）单独开关
- **依赖联动**：勾选 openviking-context-service 自动带 openviking-server
- **版本号自动获取**：从 `deploy/services/*.yaml` 的 image 字段解析，无需手动维护
- **配置自动同步**：只把各服务需要的配置复制到 NFS（按服务映射）
- **openviking 自动初始化**：部署后自动创建 agent-service 用户并生成 `api_key`
- **配置检查**：部署前检查 ov.conf 等必需文件是否就绪

### 手动部署（WSL）

```bash
# 1. 初始化 NFS
sudo bash deploy/setup-nfs.sh

# 2. 同步配置到 NFS
bash deploy/sync-config.sh

# 3. 创建 PV/PVC
NFS_SERVER=<NFS-IP> NFS_ROOT=/srv/nfs/my-agent bash deploy/apply-pv.sh

# 4. 部署服务
kubectl apply -f deploy/services/
```

### 本地访问

```bash
# Web 渠道（istio 入口）
kubectl -n istio-system port-forward svc/istio-ingressgateway 8080:80
http://localhost:8080/login.html

# Dashboard 管理面板
kubectl -n agent port-forward svc/dashboard-service 5700:5700
http://localhost:5700
```

## 跨主机端口打通（集群 ↔ WSL ↔ Windows 宿主机）

### 集群 → Windows 宿主机（经 `host.docker.internal`）

| 端口 | 用途 | 监听方 | 注意 |
|------|------|--------|------|
| 11434 | model-proxy 的 tool/local-ollama 模型 + OpenViking embedding | Windows 上的 **Ollama** | 必须监听 `0.0.0.0:11434`（`OLLAMA_HOST=0.0.0.0`），否则集群访问不到 |
| 5600 | qq-llbot-service 连接 Satori | Windows 上的 **LLBot** | 必须监听 `0.0.0.0:5600`（不能是 127.0.0.1） |

> `host.docker.internal` 由 Docker Desktop 自动解析到宿主机，无需手动转发；Windows 防火墙需放行以上端口。

### 集群 → WSL

| 端口 | 用途 | 监听方 | 配置 |
|------|------|--------|------|
| 2049 + 111 | 全部 Pod 挂载 NFS PVC | WSL 的 `nfs-kernel-server`（`setup-nfs.sh` 自动安装） | `apply-pv.sh` 指定 `NFS_SERVER=<WSL IP>`，exports 默认允许全部网段（`CLIENT_ALLOW=*`） |
| 22（可改 `CLAW_EXTERNAL_VM_PORT`） | tool-runtime 经 SSH 执行 clawhub / codex | WSL 的 sshd（`tool-runtime-apply.sh` 自动安装并配 SSH key） | `CLAW_EXTERNAL_VM_HOST` 自动探测 WSL IP |

### 说明

- `host.docker.internal` 仅 Docker Desktop **单节点**场景可用；多节点集群需改用节点 IP 或 NodePort 访问宿主机服务。

## 配置清单（config/）

配置目录是**用户修改功能的唯一入口**——需要调整时改对应文件即可，无需自己创建（除自动生成的 api_key 外）。

| 文件 | 用途 | 来源 |
|------|------|------|
| `model-proxy/config/model_list.json` | 模型配置（provider / api_url / api_key / model_params） | 手动改（填真实 API Key） |
| `openviking/ov.conf` | 语义检索后端（embedding / vlm） | 手动改（填 `vlm.api_key`） |
| `openviking/root_api_key` | 项目固定（root 用户 `my_agent` 的 key） | 固定值 |
| `openviking/api_key` | agent-service 用户 key（skill 共用账户） | **部署时自动创建** |
| `orchestrator/config/agent_list.json` | 智能体配置 | 手动改 |
| `orchestrator/config/system_settings.json` | 功能开关（图片接收 / shell 限制 / 白名单） | 手动改（缺失用默认值） |
| `orchestrator/system_prompt/` | 各智能体人设（SOUL.md / TOOL.md / ...） | 手动改（缺失文件静默跳过） |
| `qq-llbot/qq_llbot_config.json` | QQ 渠道（Satori token 等） | 手动改 |

**自动创建 / 无需手动**：workspace / skill / timer-tasks / user-data 目录（启动时 mkdir）、用户 JSON（GetUser 自动创建）、dashboard 密码（默认 123456）、降级上下文目录（local_context/）。

## QQ 渠道配置

`qq-llbot-service` 通过 Satori 协议对接 LLBot：

- LLBot 运行在 Windows 宿主机，`SATORI_HOST=host.docker.internal` 直连；**LLBot 需监听 `0.0.0.0:5600`**（不能是 127.0.0.1）
- 配置文件：`config/qq-llbot/qq_llbot_config.json`（token / host / 订阅者 ID），修改后 `kubectl rollout restart deployment/qq-llbot-service -n agent`
- 数据流：`QQ 消息 → qq-llbot-service (Satori) → scheduler.CreateTask(channel="qq") → orchestrator → scheduler.SubscribeEvents(channels=["qq"]) → qq-llbot-service → QQ 回复`

## Windows 管理员面板（admin-panel）

基于 .NET 8 WPF + WebView2 的桌面管理工具。

| 页面 | 功能 |
|------|------|
| 🔌 端口转发 | 一键启停 dashboard(5700)、gateway(5210)、image-assets(5102)、istio(8080) |
| 🚀 一键部署 | 从 `deploy-all.ps1` 解析服务列表（含必要服务组合、tool-runtime 子选项），构建部署 + 配置同步 + openviking 初始化 |
| 🤖 LLBot | 启动本地 `llbot.exe`，实时日志 + 二维码同步推送 |
| 📊 Dashboard | WebView2 内嵌 `http://localhost:5700`，直接访问管理后台 |

编译：`cd admin-panel && build.bat`，产物 `admin-panel/publish/MyAgentAdminPanel.exe`。

## 目录结构

```
My_Agent_MSA/
├── services/                   # 所有微服务源码（每个含 README.md）
│   ├── agent-orchestrator-service/    # Agent 运行时编排（5300）
│   ├── task-scheduler-service/        # 任务调度（5100）
│   ├── timer-task-service/            # 定时任务（5103）
│   ├── gateway-backend-service/       # Web 网关（5210）
│   ├── qq-llbot-service/              # QQ 渠道
│   ├── model-proxy-service/           # 模型代理（5302）
│   ├── openviking-context-service/    # RAG 上下文（5301）
│   ├── tool-runtime-service/          # 工具执行（5303）
│   ├── user-service/                  # 用户信息（5104）
│   ├── frontend-service/              # Web 前端（80）
│   └── dashboard-service/             # 管理面板（5700）
├── admin-panel/                # Windows 管理面板（C# WPF）
├── config/                     # 配置（用户修改入口，同步到 NFS）
│   ├── orchestrator/           #   agent_list.json / system_settings.json / system_prompt/
│   ├── model-proxy/            #   model_list.json
│   ├── openviking/             #   ov.conf / root_api_key / api_key(自动)
│   └── qq-llbot/               #   qq_llbot_config.json
├── deploy/                     # 部署（脚本 + YAML）
│   ├── setup-nfs.sh / sync-config.sh / apply-pv.sh / tool-runtime-apply.sh
│   └── services/               # K8s 服务 YAML（含 istio/）
├── deploy-all.ps1              # 一键部署脚本（Windows）
├── README.md                   # 项目介绍（快速上手）
├── 项目介绍.md                  # 本文档
├── 常见问题处理.md / 上下文管理.md
```

## NFS 挂载说明

PV 中 NFS 配置示例：

```yaml
nfs:
  server: 172.29.219.49
  path: /srv/nfs/my-agent/workspace
```

NFS 目录结构：

```
/srv/nfs/my-agent/
├── assets/          # 静态资源（图床、背景图）
├── config/          # 各服务持久化配置（由 sync-config.sh / deploy-all.ps1 同步）
│   ├── model-proxy/config/
│   ├── openviking/
│   ├── orchestrator/
│   │   ├── config/          # agent_list.json、system_settings.json、local_context/
│   │   └── system_prompt/   # 各智能体提示词目录
│   └── qq-llbot/
├── openviking/      # OpenViking 数据
├── timer-tasks/     # 定时任务持久化
├── user-data/       # 用户数据
└── workspace/       # 工具执行 workspace（skill / users）
```
