# 常见问题处理

## 1. QQ LLBot 渠道 WebSocket 连接超时

### 现象

`qq-llbot-service` Pod 日志显示：

```
Error while connecting:
Reconnecting...
```

Satori WebSocket 连接 `qq-satori-adapter:5600` 持续超时，但：
- Windows 本地 `netstat` 确认 LLBot 监听 `0.0.0.0:5600`
- Pod 内 TCP 测试端口可达
- WebSocket 握手始终无响应

### 根因

Docker Desktop + WSL2 环境下，K8s 的三层网络叠加导致 WebSocket 回包丢失：

```
Pod (K8s)                    Docker bridge               Windows
                                                         
  ┌─────────┐     ┌──────────────────┐     ┌─────────────────┐
  │ 10.42.x │────→│ 172.19.0.1 (NAT) │────→│ 10.83.101.77    │
  │         │     │                  │     │ (物理网卡)       │
  │ 请求    │     │                  │     │                 │
  └─────────┘     └──────────────────┘     └─────────────────┘
                         ↑
                   docker-proxy
                   做 SNAT/DNAT
```

**失败路径（K8s Service + Endpoints 映射外部 IP）：**

```
Pod → kube-proxy iptables DNAT (ClusterIP → 10.83.101.77)
    → Docker bridge SNAT (Pod IP → 172.19.0.1)
    → Windows 收到 TCP SYN，回复 SYN-ACK
    → 回复包回到 Docker bridge
    → kube-proxy iptables 反向 DNAT 时，WebSocket Upgrade 响应丢失
    → 超时
```

kube-proxy 的 iptables DNAT 规则对 TCP 三次握手没问题，但 WebSocket 是 HTTP Upgrade 后的长连接，中间多一层 NAT 会导致回包路由表匹配不上。

**成功路径（使用 `host.docker.internal`）：**

```
Pod → DNS 解析 host.docker.internal → 直接得到 10.83.101.77
    → Docker bridge SNAT（无 kube-proxy DNAT）
    → Windows 收到请求
    → 回复原路返回，路径干净
    → 成功
```

### 解决方案

将 `SATORI_HOST` 设置为 `host.docker.internal`（Docker Desktop 内置 DNS，直连 Windows 宿主机）：

```yaml
# k8s_yaml/qq-llbot-service.yaml
env:
  - name: SATORI_HOST
    value: "host.docker.internal"
```

或通过 NFS 配置文件 `/srv/nfs/my-agent/config/qq-llbot/qq_llbot_config.json`：

```json
{
  "satori": {
    "host": "host.docker.internal",
    "port": 5600,
    "token": "你的token"
  }
}
```

> ⚠️ **不要**用 K8s Service + Endpoints 映射外部 IP 来对接 WebSocket 服务，Docker Desktop 下会丢包。

### 前提条件

Windows 上 LLBot Satori 必须监听 `0.0.0.0:5600`（不能是 `127.0.0.1`），否则 Docker bridge 过来的流量会被拒绝。

## 2. 配置不生效

### 现象

修改了 NFS 上的配置文件（`/srv/nfs/my-agent/config/...`），但 Pod 内读取到的仍是旧值。

### 原因

K8s 使用 `subPath` 挂载 PVC 时，Pod 启动时缓存了文件内容，后续修改不会自动同步。

### 解决方案

```bash
kubectl -n agent rollout restart deployment/<服务名>
```

## 3. 镜像更新后 Pod 未拉取新镜像

### 现象

重新构建镜像后 Pod 仍运行旧版本。

### 原因

`imagePullPolicy: IfNotPresent` 导致节点缓存了旧镜像。

### 解决方案

将 `imagePullPolicy` 改为 `Always`，或每次构建后执行：

```bash
kubectl -n agent rollout restart deployment/<服务名>
```

## 4. 新服务镜像报 ErrImageNeverPull

### 现象

新部署的服务 Pod 状态为 `ErrImageNeverPull`：

```
Warning  ErrImageNeverPull  Container image "agent/xxx:v1" is not present with pull policy of Never
```

但 `docker images` 确认镜像已构建。

### 根因

Docker Desktop 的 K8s 运行时是 **containerd**，不是 Docker。`docker build` 构建的镜像在 Docker 命名空间，containerd 看不到。

### 解决方案

将 `imagePullPolicy` 从 `Never` 改为 `IfNotPresent`：

```yaml
spec:
  containers:
    - name: my-service
      image: agent/my-service:v1
      imagePullPolicy: IfNotPresent   # 不要用 Never
```

然后 apply 并重启：

```bash
kubectl apply -f deploy/services/my-service.yaml
kubectl -n agent rollout restart deployment/my-service
```

## 5. 中文用户名导致 gRPC 调用失败

### 现象

前端发送消息后提示：

```
任务失败：agent-orchestrator-service 调用失败: not all arguments converted during string formatting
```

### 根因

gRPC 协议中 metadata 和 protobuf 字段对非 ASCII 字符（如中文）支持有限。中文用户名在 gRPC 调用链（gateway → task-scheduler → orchestrator）中传递时，部分环节使用 `%` 格式化或 ASCII-only 的 header 校验，导致 `not all arguments converted during string formatting` 或 `Illegal header value` 错误。

### 解决方案

前端已添加用户名校验（仅允许字母、数字、下划线，最长 20 字符），但如果你通过 API 直接调用，请确保 `user_id` 只包含 `[a-zA-Z0-9_]`。（经检验 该校验实际未生效）

## 6. tool-runtime-service 中 /app/system_prompts 的实际含义

### 现象

在 `tool-runtime-service` 容器中看到 `/app/system_prompts` 目录，以为只存放系统提示词。

### 根因

`tool-runtime-service` 的部署脚本 `deploy/tool-runtime-apply.sh` 中：

```yaml
volumes:
  - name: system-prompts
    persistentVolumeClaim:
      claimName: my-agent-config-pvc
volumeMounts:
  - name: system-prompts
    mountPath: /app/system_prompts
```

PVC `my-agent-config-pvc` 对应的 PV 路径是 `${NFS_ROOT}/config`（即 `/srv/nfs/my-agent/config`）。

**容器内叫 `system_prompts` 只是挂载点的命名，实际内容是整个 NFS `config/` 目录**，包含所有服务的配置和系统提示词：

```
/srv/nfs/my-agent/config/          ← 容器内 /app/system_prompts/
├── model-proxy/config/model_list.json
├── openviking/ov.conf, api_key
├── orchestrator/config/agent_list.json
├── orchestrator/system_prompt/...  ← 各 agent 的 SOUL.md 等
├── qq-llbot/qq_llbot_config.json
└── tool-runtime/config/            ← 空目录，留给 tool-runtime 自身配置
```

> ⚠️ `tool-runtime-service` 代码中并未引用 `/app/system_prompts`，该挂载仅用于读取 OpenViking API key 等配置（如 `OPENVIKING_API_KEY` 默认路径指向 `/app/system_prompts/openviking/api_key`）。

## 7. OpenViking 报 embedding / 意图分析超上下文

### 现象

`openviking` Pod 日志出现两类错误：

```
ValueError: Failed to parse intent analysis response
...
openviking.storage.collection_schemas - ERROR - Failed to generate embedding:
OpenAI API error: Error code: 400 - the input length exceeds the context length
(uri=viking://user/<某用户>/memories/events/... level=2 context_type=memory)
```

### 根因

两个报错同源：**写入 OpenViking 会话的内容没有长度上限 + 一次归档打包的条数太多**。

1. **embedding 超限（L2 详情层）**：`append_turn` 按 `commit_limit` 攒够条数后调 `commit_session`，OpenViking 会把这一整批消息的 ChatLog 写进**一个**事件记忆文件（`memories/events/...`，L2 详情层存完整原始内容）。条数越多文件越大：例如 30+ 条对话 + Summary 约 19KB 中文 ≈ 上万 token，超过 embedding 模型上下文（`nomic-embed-text` 为 8192 token），embedding 请求被模型拒绝，且 `INPUT_TOO_LARGE` 会被**直接丢弃、不重试**。
2. **意图分析超限**：`search_context` 回退到 `client.search()` 时，OpenViking 的 intent analyzer 会把「压缩摘要 + 最近 5 条消息 + 当前 query」拼成 Prompt 喂给 VLM，超长时返回 400，JSON 解析失败即报 `Failed to parse intent analysis response`。

> ⚠️ `SemanticConfig` 里的 `abstract_max_chars` / `overview_max_chars` / `max_file_content_chars` 等参数只控制 **L0 摘要 / L1 概览的生成**，管不到 L2 详情层的 embedding 与意图分析，改了也无效。

### 定位方法

查看工作区中最大的记忆文件，超过 ~8KB 的中文文件就可能超限：

```bash
kubectl -n agent exec <openviking-pod> -- sh -c \
  "find /data/openviking/workspace -name '*.md' -printf '%s %p\n' | sort -rn | head -10"
```

### 解决方案

核心思路：**把一次归档拆小，而不是截断内容**。

1. **调小 `commit_limit`**（`config/orchestrator/config/agent_list.json`）：角色扮演/长对话类 agent 从 30 调到 10~12（15 条中文内容已接近 8192 token 上限，边际较紧）。改动后 `kubectl -n agent rollout restart deployment/agent-orchestrator-service`。
2. **提交时保留最近消息**（代码改动，需重 build `openviking-context-service`）：`append_turn` 调 `commit_session` 时传 `keep_recent_count=10`，只归档更早的消息，最近 10 条留在活跃会话，单次事件文件更小且上下文不丢。
3. **意图分析绕开超长场景**（代码改动）：`search_context` 优先 `find`，仅当 query 较短（如 ≤800 字符）时才回退 `client.search()`，超长 query 只走 `find`，从根上消除 `Failed to parse intent analysis response`。
4. **已失败的文件不会自动重试**：确认不再需要后手动删除对应 `memories/events/.../*.md` 即可消除后续告警。
