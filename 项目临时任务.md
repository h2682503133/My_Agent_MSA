# 网关前后端改造设计方案

## 当前架构理解

```
Frontend (chat.html) → Gateway Backend (app.py:5210) 
    → gRPC → Task Scheduler (5100) → Agent Orchestrator (5302)
    ← SSE event stream ← Event Bus ← Orchestrator
```

关键发现：
- `FrontendMessage` 已有 `agent_id` 字段，scheduler proto 也有 `agent_id`，**管道已通**
- `user-service` 已有 `BindChannel`/`UnbindChannel` gRPC，可直接代理
- `tool-runtime-service` 的 workspace 按 `user_id` 隔离，可直接代理
- `agent_list.json` 在 NFS config 中，已有多用户多智能体配置

---

## 一、后端改造（gateway-backend-service）

### 1. 新增 gRPC 客户端

需要新增两个 gRPC 客户端连接其他服务：

| 新文件 | 连接服务 | 用途 |
|---|---|---|
| `user_client.py` | user-service (gRPC) | 代理用户信息/渠道绑定 |
| `tool_client.py` | tool-runtime-service (gRPC) | 代理工作空间文件操作 |

### 2. 新增 REST API

```
用户 & 渠道
├── GET  /api/user/profile          → 获取当前用户信息（含 channels）
├── PUT  /api/user/profile          → 更新用户信息
├── POST /api/user/channels         → 绑定渠道（channel, channel_user_id, priority）
├── DELETE /api/user/channels/{channel} → 解绑渠道

工作空间
├── GET  /api/workspace/files       → 列出文件树（支持 ?path= 子目录）
├── GET  /api/workspace/files/read  → 读取文件内容（?path=）
├── POST /api/workspace/files/write → 写入文件（?path= + body）
├── DELETE /api/workspace/files     → 删除文件（?path=）

智能体
├── GET  /api/agents                → 列出可用智能体列表（读 agent_list.json）

对话管理（内存态，不持久化）
├── GET  /api/conversations         → 列出当前用户所有活跃对话
├── POST /api/conversations         → 创建新对话 {agent_id}，默认 "main"
├── DELETE /api/conversations/{agent_id} → 关闭对话

日志
├── GET  /api/logs/orchestrator     → 获取 agent-orchestrator 中当前用户的日志
```

### 3. 改造现有端点

**`POST /api/messages`** — 增强：
- 必须携带 `agent_id`（前端传入）
- 同一个 `user_id` + `agent_id` 组合复用同一个 SSE 流
- `session_id` 改为 `{user_id}_{agent_id}` 格式，实现按智能体隔离

**`GET /api/events`** — 增强：
- 增加 `agent_id` 查询参数
- SSE 事件流按 `user_id + agent_id` 过滤

### 4. 对话管理结构

```python
# 内存态，服务重启即丢失
conversations: dict[str, dict[str, Conversation]]
# 结构: {user_id: {agent_id: Conversation}}
# Conversation: {agent_id, messages: list, created_at, last_active}
```

`/api/conversations` 创建新对话时只需传 `agent_id`，后端自动初始化并返回。关闭对话只清理内存中的消息列表，不影响服务端任何状态。

### 5. 工作空间文件访问

通过 gRPC 代理到 `tool-runtime-service`：
- 默认 workspace 根目录按 `user_id` 隔离（`/app/workspace/users/{user_id}`）
- 其他渠道的 workspace 可通过 `/api/workspace/files?user_id=qq_xxx` 跨用户访问（需校验权限或白名单）

### 6. 日志获取

两种方案：
- **方案 A（推荐）**：后端通过 `kubectl logs` 或 K8s API 读取 `agent-orchestrator-service` pod 日志，用 `grep user_id` 过滤
- **方案 B**：在 orchestrator 侧新增一个 gRPC 端点返回某用户的日志

推荐方案 A，因为不需要改 orchestrator。

---

## 二、前端设计思路（仅需求，不写代码）

### 整体布局：三栏式

```
┌──────────┬──────────────────────┬─────────────┐
│  侧边栏   │      主对话区         │  右侧面板    │
│          │                      │  (可折叠)    │
│ 对话列表  │   Chat Messages      │  用户设置    │
│ + main   │                      │  工作空间    │
│ + tool   │   [消息气泡...]       │  日志查看    │
│ + 新建... │                      │             │
│          │  ┌─────────────────┐ │             │
│ 用户信息  │  │ 消息输入框       │ │             │
│ 退出登录  │  └─────────────────┘ │             │
└──────────┴──────────────────────┴─────────────┘
```

### 侧边栏
- **对话列表**：按 `agent_id` 分组，显示每个智能体的对话入口
- **新建对话按钮**：弹出输入框要求输入 `agent_id`，默认填充 `main`
- **当前用户信息**：显示 user_id，点击进入设置面板

### 主对话区
- 顶部显示当前智能体名称和 agent_id
- 消息气泡（用户/agent/system 三种样式）
- 底部消息输入框，支持 Enter 发送
- 通过 SSE 实时接收事件流

### 右侧面板（Tab 切换）
1. **用户设置 Tab**：
   - 用户基本信息编辑
   - 渠道绑定管理（列表展示已绑定的 QQ/微信等，支持添加/删除）
   - 每个渠道显示：渠道名、渠道用户 ID、优先级

2. **工作空间 Tab**：
   - 文件树浏览器（左侧目录树 + 右侧文件内容预览）
   - 支持切换查看自己或其他渠道用户的工作空间
   - 文件操作：读取、写入、删除（基础文本文件）

3. **日志 Tab**：
   - 显示当前用户在 agent-orchestrator 中的相关日志
   - 支持自动刷新和关键字过滤

### 多对话管理
- 侧边栏显示所有活跃对话，按 `agent_id` 区分
- 点击切换对话时，对话区内容切换（纯前端切换，SSE 流按 `user_id` 全局订阅，前端按 `agent_id` 过滤事件）
- 不需要后端持久化对话记录，刷新页面后对话列表从空开始（未来 App 可调用网关 API 自己管理持久化）

---

## 三、文件改动清单

| 文件 | 改动类型 | 说明 |
|---|---|---|
| `gateway-backend-service/app.py` | 改造 | 新增所有 REST 端点，改造现有端点 |
| `gateway-backend-service/schemas.py` | 改造 | 新增请求/响应模型 |
| `gateway-backend-service/sse_hub.py` | 改造 | 支持按 agent_id 过滤事件 |
| `gateway-backend-service/scheduler_client.py` | 改造 | 传递 agent_id |
| `gateway-backend-service/user_client.py` | **新增** | user-service gRPC 客户端 |
| `gateway-backend-service/tool_client.py` | **新增** | tool-runtime gRPC 客户端 |
| `gateway-backend-service/conversations.py` | **新增** | 内存态对话管理 |
| `gateway-backend-service/requirements.txt` | 改造 | 可能新增依赖 |
| `gateway-backend-service/Dockerfile` | 改造 | 复制新文件 |
| `gateway-backend-service/proto/` | 新增 proto | user.proto, tool_runtime.proto |
| `frontend-service/chat.html` | 改造 | 三栏布局 + 多对话 + 面板 |

---

## 四、关键技术决策

1. **对话不持久化**：`conversations.py` 纯内存 dict，服务重启即丢失，未来 App 自己维护
2. **SSE 按 user_id 全局订阅**：后端维持一个 SSE 流，前端按 `agent_id` 过滤事件，避免多连接开销
3. **gRPC 代理模式**：gateway 对 user-service 和 tool-runtime 的调用走 gRPC 代理，保持微服务解耦
4. **日志读取**：通过 `kubectl logs` 管道读取，不修改 orchestrator 代码

---

## 五、补充需求：工作空间文件预览

### 文本预览
- 工作空间文件浏览器中，点击文本文件（.txt / .md / .json / .yaml / .py / .log 等）时，右侧预览区展示文件内容
- 预览区顶部提供**编码选择下拉框**：UTF-8（默认）、GBK、GB2312、Latin-1 等
- 切换编码时前端重新请求 `/api/workspace/files/read?path=xxx&encoding=gbk`
- 后端 API 支持 `encoding` 参数，默认 utf-8

### 图片预览
- 工作空间文件浏览器中，点击图片文件（.png / .jpg / .gif / .webp / .svg / .bmp 等）时，右侧预览区直接展示图片
- 后端新增 `/api/workspace/files/raw?path=xxx` 端点，返回原始文件字节流 + 正确的 Content-Type
- 图片过大时（>5MB），前端显示缩略图 + 文件大小提示，点击可新标签页打开原图
