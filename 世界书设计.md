# 世界书（World Info / Lorebook）设计

> 目标：用 openviking-context-service 实现类似 SillyTavern「世界书」的功能——
> 手写静态世界知识条目，按**关键词确定性触发**注入对话上下文。
> 本文档为设计方案，状态：**已实现**（2026-08）——与 PROCESS、openviking 三者共同管理上下文，
> 智能体可通过工具自动更新世界书。

---

## 1. 背景：世界书机制 vs openviking 现有能力

SillyTavern 世界书（[官方文档](https://docs.sillytavern.app/usage/core-concepts/worldinfo/)）的核心：

- **条目（Entry）**：`关键词(keys)` + `内容(content)`，可选 `常驻(constant)` / `优先级(priority)` / `启用开关(enabled)` / `正则(regex)`
- **触发**：对话文本中出现关键词 → 该条目被**确定性激活**（精确匹配/正则，不依赖向量相似度）
- **注入**：命中条目按优先级排序、token 预算裁剪后拼成一段注入提示词

| 能力 | openviking 现有接口 | 世界书对应物 | 差距 |
|---|---|---|---|
| 会话摘要 + 自动记忆 | `SearchContext`（语义检索） | 无 | 世界书不自动提炼 |
| 手写静态知识 | `SearchSkillDocs`（向量检索） | ✅ 最接近 | 触发方式是向量匹配，不可控 |
| 常驻设定 | 无专用机制（靠 `SOUL.md` 静态注入） | constant 条目 | 无开关/优先级 |
| 关键词确定性触发 | ❌ | 世界书灵魂 | **缺失，本次补上** |

**结论**：openviking 现有接口只能做「语义版世界书」，做不出「关键词触发版世界书」。
且当前环境 embedding 已降级为 `keyword_recall`（ollama 已停），语义检索退化，更不可控。
因此需要一个独立的、不依赖 embedding 的关键词匹配模块。

---

## 2. 总体架构与数据流

```
用户消息 ──→ agent-orchestrator-service
                 │  AgentRuntime.send()
                 ├─ context_client.search_context()   （现有：摘要+记忆+最近消息）
                 ├─ context_client.search_world_info()（世界书命中）──gRPC──→ openviking-context-service
                 │                                          └─ world_info.py 读 world_info.json（NFS config）
                 │                                            关键词匹配 → 排序 → token 裁剪 → 返回 hits
                 ├─ tool-runtime worldinfo-write/remove/list （智能体自动更新世界书）
                 │     └─ 直接写 NFS world_info.json（原子写 + .bak 备份）
                 └─ 组装 messages：追加【世界设定】system 段
```

**分工原则**：

| 职责 | 落在哪 | 理由 |
|---|---|---|
| 读文件 + 关键词匹配 + 裁剪 | openviking-context-service（`SearchWorldInfo` gRPC） | 与记忆检索同层，符合「利用 openviking」 |
| 注入组装 | agent-orchestrator-service `context_client` + `send()` | 与【会话摘要】注入同链路 |
| 条目管理（增删改查） | ① orchestrator 内置命令（`世界书:add` 等）② tool-runtime 工具（`worldinfo-*`，智能体自动更新） | 两路都直接写 NFS 文件；orchestrator 与 tool-runtime 的 config 挂载均可写 |

> 关键事实（已核对 deploy 配置）：
> - `my-agent-config-pv` → NFS `${NFS_ROOT}/config`
> - agent-orchestrator-service：`/app/config` ← subPath `orchestrator/config`（**可写**，PROCESS 目录即写在这里）
> - tool-runtime-service：`/app/system_prompts` ← 整个 config 根（**可写**），`WORLD_INFO_DIR=/app/system_prompts/orchestrator/config/world_info`
> - openviking-context-service：`/app/config` ← 整个 config 根（**只读**，读 world_info.json 足够）
> - dashboard-service：`/app/config` ← 整个 config 根，配置管理页可直接编辑 `orchestrator/config/world_info/world_info.json`
> - **存储位置**：`${NFS_ROOT}/config/orchestrator/config/world_info/`（与 PROCESS 同级，而非文档早期版本的 `${NFS_ROOT}/config/world_info/`——后者 orchestrator 的 subPath 挂载看不到）

---

## 3. 数据模型

### 存储位置

```
${NFS_ROOT}/config/orchestrator/config/world_info/
├── world_info.json      # 条目（orchestrator 侧 /app/config/world_info/world_info.json）
└── groups.json          # 群组映射（agent_id : 群组id）
```

（openviking 侧路径：`/app/config/orchestrator/config/world_info/`；orchestrator 侧 `/app/config/world_info/`；tool-runtime 侧 `/app/system_prompts/orchestrator/config/world_info/`——同一份 NFS 文件。）

### JSON 结构

```json
{
  "version": 1,
  "entries": [
    {
      "id": "wi_1717000000000",
      "scope": "main",
      "keys": ["王都", "皇城"],
      "content": "王都是大陆最大的城市，王宫坐落于城北……",
      "priority": 10,
      "constant": false,
      "regex": false,
      "enabled": true,
      "created_at": "2026-02-01T10:00:00",
      "updated_at": "2026-02-01T10:00:00"
    }
  ]
}
```

### groups.json（群组映射，agent_id : 群组id）

```json
{
  "agent_groups": {
    "main": ["核心角色"],
    "writer": ["核心角色", "创作组"],
    "tool": ["工具组"]
  }
}
```

加载 agent 的世界书条目 = scope 等于该 agent_id 的条目 ∪ scope 等于该 agent 所属群组的条目。

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | string | 是 | 唯一标识，`wi_<毫秒时间戳>`（管理工具自动生成） |
| `scope` | string | 是 | **无全局作用域**：`agent_id`（默认，仅该智能体可见）或 `group:群组id`（群组内所有智能体可见）；空/缺失条目不参与匹配（手写须显式填） |
| `keys` | string[] | 是（constant 可空） | 触发关键词，子串匹配，大小写不敏感 |
| `content` | string | 是 | 注入内容（世界设定文本） |
| `priority` | int | 否 | 优先级，默认 0；数值大者先注入（token 紧张时先被保留） |
| `constant` | bool | 否 | 常驻条目：不靠触发，每次对话轮直接注入 |
| `regex` | bool | 否 | keys 按正则 `re.search` 匹配 |
| `match_mode` | string | 否 | `or`（默认，任一关键词命中即触发）/ `and`（全部关键词命中才触发）；非法值回退 or |
| `enabled` | bool | 否 | 开关，默认 true；false 条目不参与匹配 |
| `created_at` / `updated_at` | string | 否 | 时间戳，ISO 格式 |

---

## 4. 匹配与注入算法（`SearchWorldInfo` 语义）

输入：`query`（当前用户消息）、`recent_messages`（最近 N 条消息）、`max_tokens`、`max_entries`、`agent_id`。

1. **过滤**：`enabled == true` 且 `scope` 可见——`scope == agent_id` 或 `scope == "group:<群组id>"` 且该群组包含当前 agent（groups.json 的 `agent_groups`）；空 scope 一律不匹配（无全局）。
2. **常驻候选**：`constant == true` 的条目全部入选。
3. **触发候选**：对 `query` + `recent_messages` 逐条文本：
   - `regex == false`：关键词按**子串**匹配（大小写不敏感）
   - `regex == true`：关键词按正则 `re.search` 匹配（写错的正则视为不匹配，静默跳过）
   - `match_mode == or`（默认）：**任一**关键词命中即触发
   - `match_mode == and`：**全部**关键词命中才触发（正则模式同理，所有正则都匹配才触发）
   - 同一条目多个 key 命中只记一次；同一 key 在多条消息命中只记一次
4. **排序**：入选集合按 `priority` 降序（同优先级按 `updated_at` 新者在前）。
5. **裁剪**：按 `max_entries` 上限截断，再按 `max_tokens` 预算从高优先级起累计，超出截断。
6. 返回 `hits`（含 entry_id / keys / content / priority / constant / token_count）。

### 本轮去重（关键）

`send()` 在**每次模型调用**都会执行（含工具回传轮、agent 转交轮）。若每轮都注入世界书，会重复消耗 token。

**默认策略**：世界书两阶段只在「用户原始请求轮」执行（`caller == user` 且无图片），工具回传轮/agent 转交轮保持单次调用、不注入。
（与 `search_context` 的行为对齐——同样是组装进 long_context，靠模型上下文窗口自然承载。）

### 注入格式

```text
【世界设定】
- 王都：王都是大陆最大的城市，王宫坐落于城北……
- 皇室：皇室掌握着魔法的垄断权……
```

多条命中合并为一个 system 消息；无命中则不注入空段（与 PROCESS 的空段策略一致）。

---

## 4b. 两阶段世界书（智能体词义感知，v61+）

**目标**：让智能体在**生成回复时**手边有"这轮要用的词"的定义——不是等用户提到才注入，而是它自己想说的词也能拿到含义。

**机制（仅用户请求轮 + files 声明 world_info.md + 无图片）**：

```
阶段1：messages + 列词指令（最后一条 system，紧贴用户请求）
  → 模型输出【概念词】词1、词2、词3（不写回复正文）
  （原生 reasoning_content 由 model-proxy 透传，供阶段2 引用）
阶段2：messages + 【你的思考】(reasoning) + 【世界设定·命中】 → 生成最终回复
```

**触发源 = 【概念词】清单 ∪ 阶段1 输出全文 ∪ 用户消息（query）**：
- 【概念词】清单：模型主动声明"这轮要用这些词"，最准——区分"说词"与"说内容"交给模型自己
- 阶段1 全文：模型没配合列词、直接生成闲聊时，闲聊内容也参与扫描（"闲聊扫描"兜底）
- 用户消息：模型漏列时兜底

**降级（不中断流程）**：
- 阶段1 调用失败 / 无输出 → 直接单次调用（不注入世界书）
- 阶段2 调用失败 → 降级单次调用
- 阶段1 直接输出了完整回复 → 也当闲聊扫描，命中注入后阶段2 精修

**开关：虚假 world_info.md**（与 PROCESS.md 同款机制）：
- `agent_list.json` 该 agent 的 `files` 含 `world_info.md` → 启用两阶段世界书
- 不含 → **完全跳过世界书**（连单次触发注入也不做）
- `world_info.md` 是声明项：build_system_prompt 跳过读取，不需要真实文件

**代价**：启用 agent 的每次用户请求 = 两次模型调用（延迟约翻倍）；thinking 占几百 token。工具/转交轮不受影响。

---

## 5. gRPC 接口（proto 增量）

文件：`proto/openviking_context.proto`（两个服务仓库内各一份，同步修改）

```proto
// 世界书检索（新增）
message SearchWorldInfoRequest {
  string user_id = 1;
  string agent_id = 2;
  string query = 3;                    // 当前用户消息
  repeated string recent_messages = 4; // 最近消息（触发源）
  int32 max_tokens = 5;                // 注入 token 预算
  int32 max_entries = 6;               // 最多注入条数
}
message WorldInfoHit {
  string entry_id = 1;
  repeated string keys = 2;
  string content = 3;
  int32 priority = 4;
  bool constant = 5;
  int32 token_count = 6;
}
message SearchWorldInfoResponse {
  repeated WorldInfoHit hits = 1;
  string error = 2;
}

// 追加到 OpenVikingContext service
rpc SearchWorldInfo(SearchWorldInfoRequest) returns (SearchWorldInfoResponse);
```

> 说明：管理接口（增删改查）**不走 gRPC**——orchestrator 直接读写 NFS 文件
> （openviking-context-service 的 config 挂载是只读，写接口会破坏该设计）。

---

## 6. 服务端实现清单（openviking-context-service）

| 文件 | 改动 |
|---|---|
| `app/config.py` | 新增 `WORLD_INFO_PATH`（env `WORLD_INFO_PATH`，默认 `/app/config/world_info/world_info.json`）、`WORLD_INFO_MAX_TOKENS`（默认 1500）、`WORLD_INFO_MAX_ENTRIES`（默认 20） |
| `app/world_info.py`（新增） | 读文件 + mtime 缓存（stat 变化才重读，用户改 NFS 即时生效）+ 匹配/排序/裁剪逻辑，返回 hits |
| `app/server.py` | 注册 `SearchWorldInfo` handler，透传 `store.search_world_info(...)` |
| `app/viking_store.py` | `VikingStore.search_world_info()`（mock 模式返回空；server 模式不依赖 openviking-server，直接走本地 world_info.py） |
| `scripts/gen_proto.sh` | 无改动（proto 文件更新后重跑） |

> 关键点：世界书匹配是**纯本地文件 + 正则**，不依赖 openviking-server / embedding，
> 因此 embedding 降级（keyword_recall）不影响本功能。

---

## 7. 注入点实现清单（agent-orchestrator-service）

| 文件 | 改动 |
|---|---|
| `app/context_client.py` | 新增 `search_world_info(user_id, agent_id, query, recent_messages, max_tokens)` → gRPC 调用，失败静默降级返回空（同 `search_context` 的降级风格） |
| `app/agent_runtime.py` `send()` | 两阶段调用（用户请求轮 + world_info.md 开关）：阶段1 列概念词 → 匹配 → 阶段2 注入生成；失败降级单次 |
| `app/agent_runtime.py` | `_world_info_mode_from_files` 开关检测；`_extract_concepts` 概念词提取；`_format_world_info_block` 注入段 |
| `app/config.py` | `WORLD_INFO_*` 路径/预算配置、`WORLD_INFO_ENABLED` 总开关 |

### 两阶段调用结构（用户请求轮 + files 含 world_info.md）

```
阶段1：messages + 列词指令（最后一条 system，紧贴用户请求）
  → 模型输出【概念词】词1、词2、词3（或闲聊正文）
  触发源 = 【概念词】清单 ∪ 阶段1 全文 ∪ 用户消息
  命中 → hits
阶段2：messages + 【你的思考】(reasoning 透传) + 【世界设定·命中】(hits)
  → 模型生成最终回复（走现有 parse_syntax 流程）
降级：阶段1/2 失败或无输出 → 直接单次调用（不注入世界书，不中断流程）
```

（详情见 §4b。注入格式：命中条目拼成 system 消息，插在系统提示词之后、用户输入之前。）

---

## 8. 管理入口（两路，都直接原子写 NFS）

### 8.1 用户口头命令（orchestrator 内置协议命令）

与 `定时任务:` 同级的新协议命令，syntax_parser 增加分支，处理函数直接原子写 NFS 文件
（复用 PROCESS 的 `_atomic_write_text` 模式；**写前自动备份 `world_info.json.bak`**，符合 NFS 改动红线）。

```text
世界书:add|关键词1,关键词2|内容|优先级|scope|constant|regex|match_mode   # 添加条目（scope 省略=当前 agent；群组用 group:群组id；match_mode=or/and）
世界书:list                                        # 列出全部条目（id/关键词/内容前 50 字/常驻/优先级）
世界书:update|条目id|新内容                        # 更新内容
世界书:delete|条目id                               # 删除条目
世界书:enable|条目id|true|false                    # 开关
```

示例（对话里对 main 说）：

```text
记住世界设定：世界书:add|王都,皇城|王都是大陆最大的城市，王宫坐落于城北，由皇室统治。
```

### 8.2 智能体自动更新（tool-runtime 内置工具，复用 process-write 成熟模式）

模型发现新的长期设定时**主动调用工具**写入世界书，与 PROCESS 的 process-write 同一心智：

```text
worldinfo-write|关键词1,关键词2|内容|优先级|scope|constant|regex|match_mode   # scope 省略=当前 agent（对话主智能体）；match_mode=or/and
worldinfo-remove|条目id                                            # 删除
worldinfo-list                                                     # 列出全部（模型自查"已有哪些设定"，防止重复写）
```

- 写 NFS 与 PROCESS 同款原子写 + 自动 `.bak` 备份
- **防滥用约束**：同 scope + 相同关键词集合 → 覆写内容而非新增（防垃圾堆积）；条目内容为空拒绝
- 执行结果回显给模型（写成功/失败可见），与 process-write 行为一致
- 用户已确认**不需要**自动写入总开关（WORLD_INFO_AUTO_WRITE 不实现）

### 8.3 模型引导（提示词层，自动更新生效的关键）

在 `GLOBAL_SETTING.md`（或 main/SOUL.md）追加说明：
「发现新的长期设定（角色/地点/关系/规则，跨会话不变）→ 用 `worldinfo-write` 写入世界书；
本次任务进行中的进展 → 用 `process-write` 写 PROCESS；写前先 `worldinfo-list` 查重。」

---

## 9. 部署与运维

- **无新增 PVC**：复用 `my-agent-config-pvc`（NFS `${NFS_ROOT}/config`），无新增挂载/权限改动
- **openviking-context-service**：新增 `app/world_info.py` + `SearchWorldInfo` gRPC handler；挂载不变（`/app/config` 只读即可），重新 build + `kubectl set image`
- **agent-orchestrator-service**：新增 context_client.search_world_info + send 注入 + `世界书:*` 协议命令 + world_info_manager.py，重新 build + `kubectl set image`
- **tool-runtime-service**：新增 worldinfo_runtime.py + 工具注册（`WORLD_INFO_PATH` env 默认已指向正确路径），重新 build + `kubectl set image`
- **dashboard-service**：修复全局 system prompt 文件（GLOBAL_SETTING.md 等）无法编辑/删除的 bug（前端 spSave/spDeleteFile 允许空 agent；后端写回原位置、删除支持 global），重新 build + `kubectl set image`
- **proto**：两服务各自仓库内 `proto/openviking_context.proto` 同步修改 + `gen_proto.sh` 重新生成（Docker build 时自动执行）
- **NFS 备份惯例**：管理工具每次写前自动备份 `.bak`；用户手改前自行 `cp world_info.json world_info.json.bak`（与 SOUL.md 一致）
- **热加载**：world_info.py 按文件 mtime 缓存失效，用户改 NFS 后下一次请求即生效，无需重启

---

## 10. 与现有机制的分工（一张表）——三者共同管理上下文

| 机制 | 载体 | 写入方 | 注入时机 | 定位 |
|---|---|---|---|---|
| 常驻人格/总规则 | `GLOBAL_SETTING.md` / `SOUL.md` | 用户手改 / dashboard | 每轮 | 全局不可开关的常驻设定 |
| 世界书 | `world_info.json` | 用户命令 / **智能体自动（worldinfo-* 工具）** | 仅用户原始请求轮 + 关键词触发 | **按关键词触发的静态长期知识**（跨会话稳定，本次新增） |
| PROCESS 事件记录 | `config/process/*.json` | 智能体主动（process-* 工具） | 每轮 | 运行期动态事件（任务进展/线索），随会话滚动 |
| openviking 记忆 | openviking-server session | 系统自动（append_turn） | 每轮 | 自动提炼的对话历史摘要 + 语义记忆（模型不可控） |
| skill 文档 | openviking skill | 系统/工具 | 按需 | 工具/技能说明（面向 tool-runtime） |

**三者互补关系**：
- 世界书补 PROCESS 的「会忘」——PROCESS 是滚动条，世界书是模型主动沉淀的跨会话稳定层
- 世界书补 openviking 的「不可控」——openviking 自动摘要模型看不见，世界书是模型主动判定写入的确定性知识
- PROCESS 补世界书的「太静态」——世界书仅首轮 + 关键词触发，任务中途状态变化由 PROCESS 每轮承载
- openviking 兜底两者——模型忘了写也有自动摘要

---

## 11. 边界与后续扩展

- **性能**：子串匹配 O(条目 × 关键词 × 消息数)，条目 < 1000 无压力；`regex:true` 条目建议限制数量（如 ≤ 50）并在文档标注
- **作用域**：已支持 agent_id + 群组（groups.json）；后续可扩展为按用户（`user_id`）过滤
- **分组/选择性（ST 的 group/selective）**：群组已实现；更细的选择性暂不做，`keys` 已覆盖绝大多数场景
- **条目级注入位置（ST 的 before/after char def）**：暂不做，默认固定位置；后续可在条目加 `position` 字段
- **token 预算**：世界书独立预算（默认 1500），避免挤占记忆预算；与 `search_context` 的 `max_tokens` 互不影响
- **重复注入**：仅用户原始请求轮注入（§4），工具回传/agent 转交轮不重复
- **embedding 无关**：本功能不依赖向量检索，ollama 停用不影响
