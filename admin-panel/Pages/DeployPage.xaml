<Page x:Class="MyAgentAdminPanel.Pages.DeployPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="一键部署">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="一键部署"
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" x:Name="BtnDeploy"
                    Content="🚀 构建并部署"
                    Width="140" Height="36"
                    Background="#0078D4" Foreground="White"
                    FontSize="14" FontWeight="SemiBold"
                    BorderThickness="0" Cursor="Hand"
                    Click="BtnDeploy_Click"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：服务列表 -->
            <Border Grid.Column="0" Background="White" CornerRadius="8"
                    BorderBrush="#E0E0E0" BorderThickness="1" Padding="12">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="选择服务"
                               FontSize="14" FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    <ScrollViewer>
                        <ItemsControl x:Name="ServiceList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,4" Padding="8,4"
                                            CornerRadius="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0"
                                                      IsChecked="{Binding IsSelected}"
                                                      VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding Name}"
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                            <Border Grid.Column="2"
                                                    Background="#E8F5E9"
                                                    CornerRadius="3" Padding="6,2">
                                                <TextBlock Text="{Binding Tag}"
                                                           FontSize="11"
                                                           Foreground="#2E7D32"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!-- 右侧：日志输出 -->
            <Border Grid.Column="1" Margin="16,0,0,0"
                    Background="#1E1E1E" CornerRadius="8">
                <RichTextBox x:Name="LogOutput"
                             IsReadOnly="True"
                             FontFamily="Consolas" FontSize="12"
                             Foreground="#D4D4D4"
                             Background="Transparent"
                             BorderThickness="0" Margin="12"
                             VerticalScrollBarVisibility="Auto"/>
            </Border>
        </Grid>
    </Grid>
</Page>
