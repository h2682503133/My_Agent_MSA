<Window x:Class="MyAgentAdminPanel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MyAgent 管理控制面板"
        Width="1100" Height="700"
        MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavButtonActive" TargetType="Button" BasedOn="{StaticResource NavButton}">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧导航栏 -->
        <Border Grid.Column="0" Background="#1E1E2E">
            <StackPanel Margin="0,16,0,0">
                <TextBlock Text="MyAgent Panel"
                           FontSize="18" FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <Button x:Name="BtnPortForward" Content="🔌 端口转发"
                        Style="{StaticResource NavButtonActive}" Click="NavButton_Click"/>

                <Button x:Name="BtnDeploy" Content="🚀 一键部署"
                        Style="{StaticResource NavButton}" Click="NavButton_Click"/>

                <Button x:Name="BtnLLBot" Content="🤖 LLBot"
                        Style="{StaticResource NavButton}" Click="NavButton_Click"/>

                <Button x:Name="BtnDashboard" Content="📊 Dashboard"
                        Style="{StaticResource NavButton}" Click="NavButton_Click"/>
            </StackPanel>
        </Border>

        <!-- 右侧主体内容 -->
        <Frame Grid.Column="1" x:Name="ContentFrame"
               NavigationUIVisibility="Hidden"
               Background="#F3F3F3"/>
    </Grid>
</Window>
